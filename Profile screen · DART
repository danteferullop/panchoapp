import 'package:flutter/material.dart';
import '../services/auth_service.dart';
import '../theme/app_theme.dart';
import '../widgets/glass_card.dart';
import 'orders_list_screen.dart';

class ProfileScreen extends StatelessWidget {
  const ProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final uid = AuthService.instance.currentUserId ?? '—';

    return Scaffold(
      appBar: AppBar(title: const Text('PERFIL')),
      body: ListView(
        padding: const EdgeInsets.all(20),
        children: [
          Center(
            child: Container(
              width: 96,
              height: 96,
              padding: const EdgeInsets.all(3),
              decoration: const BoxDecoration(
                shape: BoxShape.circle,
                gradient: AppColors.primaryGradient,
              ),
              child: const CircleAvatar(
                backgroundColor: AppColors.bgPrimary,
                child: Icon(Icons.person_rounded, size: 42, color: AppColors.accentGlossy),
              ),
            ),
          ),
          const SizedBox(height: 12),
          Center(
            child: Text(
              'Invitado',
              style: Theme.of(context).textTheme.titleMedium,
            ),
          ),
          Center(
            child: Text(
              'ID de sesión: ${uid.length > 10 ? uid.substring(0, 10) : uid}…',
              style: Theme.of(context).textTheme.bodyMedium,
            ),
          ),
          const SizedBox(height: 28),
          GestureDetector(
            onTap: () => Navigator.of(context).push(
              MaterialPageRoute(builder: (_) => const OrdersListScreen()),
            ),
            child: GlassCard(
              child: Row(
                children: [
                  const Icon(Icons.receipt_long_rounded, color: AppColors.accentGlossy),
                  const SizedBox(width: 14),
                  Expanded(
                    child: Text('Mis pedidos', style: Theme.of(context).textTheme.titleMedium),
                  ),
                  const Icon(Icons.chevron_right_rounded, color: AppColors.accentGlossy),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }
}
